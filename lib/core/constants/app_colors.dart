import 'package:flutter/material.dart';

class AppColors {
  /// Equivalente a `Colors.grey[300]` => #FFE0E0E0
  static const Color grey300 = Color(0xFFE0E0E0);

  /// Equivalente a `Colors.grey[600]` => #FF757575
  static const Color grey600 = Color(0xFF757575);

  /// Equivalente a `Colors.grey[800]` => #FF424242
  static const Color grey800 = Color(0xFF424242);

  /// Equivalente a `Colors.blue` => #FF2196F3
  static const Color blue = Color(0xFF2196F3);

  /// Equivalente a `Colors.red` => #FFF44336
  static const Color red = Color(0xFFF44336);

  /// Equivalente a `Colors.white` => #FFFFFFFF
  static const Color white = Color(0xFFFFFFFF);

  /// Equivalente a `Colors.black` => #FF000000
  static const Color black = Color(0xFF000000);

  /// Equivalente a `Colors.amber` => #FFFFC107
  static const Color amber = Color(0xFFFFC107);

  /// Equivalente a `Colors.green` => #FF4CAF50
  static const Color green = Color(0xFF4CAF50);
}
